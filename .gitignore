# Go build artifact (28MB binary — regenerated by ./lambda/build.sh, do not commit)
lambda/bootstrap

# OpenTofu state and provider cache (do not commit)
build/
.terraform/
*.tfstate
*.tfstate.*
*.hcl

# OS
.DS_STORE
